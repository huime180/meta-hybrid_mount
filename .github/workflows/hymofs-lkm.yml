# Copyright (C) 2026 YuzakiKokuban <heibanbaize@gmail.com>
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Build HymoFS Kernel Module

on:
  workflow_call:
    inputs:
      kmi:
        description: "KMI version"
        required: true
        type: string
      arch:
        description: "Target architecture (DDK currently supports arm64 only)"
        required: true
        type: string
      hymofs_ref:
        description: "HymoFS git ref (branch/tag/commit)"
        required: false
        default: "main"
        type: string
      ddk_release:
        description: "DDK release version"
        required: false
        default: "20251104"
        type: string

jobs:
  build:
    name: Build hymofs ${{ inputs.arch }} ${{ inputs.kmi }}
    runs-on: ubuntu-latest
    env:
      KMI: ${{ inputs.kmi }}
      TARGET_ARCH: ${{ inputs.arch }}
      OUT: ${{ github.workspace }}/out
      SRC_ROOT: ${{ github.workspace }}/hymofs_src
      SRC: ${{ github.workspace }}/hymofs_src/src

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Validate target arch
        run: |
          if [ "$TARGET_ARCH" != "arm64" ]; then
            echo "DDK workflow currently supports arm64 only, got: $TARGET_ARCH"
            exit 1
          fi

      - name: Resolve HymoFS source
        id: hymofs_source
        run: |
          set -euo pipefail

          echo "Resolving HymoFS (ref: ${{ inputs.hymofs_ref }})..."
          rm -rf "$SRC_ROOT"
          git init "$SRC_ROOT"
          (
            cd "$SRC_ROOT"
            git remote add origin "https://github.com/Anatdx/HymoFS.git"
            git fetch --depth 1 origin "${{ inputs.hymofs_ref }}"
            git checkout FETCH_HEAD
            SHA="$(git rev-parse HEAD)"
            COMMIT_NUM="$(git rev-list --count HEAD)"
            VERSION="$((COMMIT_NUM + 200))"

            echo "sha=$SHA" >> "$GITHUB_OUTPUT"
            echo "version=$VERSION" >> "$GITHUB_OUTPUT"
            echo "Resolved HymoFS to $SHA"
          )
          ls -la "$SRC/"

      - name: Restore cached .ko
        id: cache-lkm
        uses: actions/cache@v5
        with:
          path: ${{ env.OUT }}
          key: hymofs-lkm-${{ runner.os }}-${{ env.KMI }}-${{ env.TARGET_ARCH }}-ddk-${{ inputs.ddk_release }}-hymofs-${{ steps.hymofs_source.outputs.sha }}-${{ hashFiles('.github/workflows/hymofs-lkm.yml') }}

      - name: Build hymofs_lkm.ko
        if: steps.cache-lkm.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail

          IMAGE="ghcr.io/ylarod/ddk:${KMI}-${{ inputs.ddk_release }}"
          HYMOFS_VERSION="${{ steps.hymofs_source.outputs.version }}"

          echo "Cache miss for ${KMI}_${TARGET_ARCH}_hymofs_lkm.ko"
          echo "Building with DDK image: $IMAGE"
          docker pull "$IMAGE"
          docker run --rm --privileged \
            -v "${GITHUB_WORKSPACE}:${GITHUB_WORKSPACE}" \
            -w "${GITHUB_WORKSPACE}" \
            -e KMI="$KMI" \
            -e TARGET_ARCH="$TARGET_ARCH" \
            -e OUT="$OUT" \
            -e SRC_ROOT="$SRC_ROOT" \
            -e SRC="$SRC" \
            -e HYMOFS_VERSION="$HYMOFS_VERSION" \
            "$IMAGE" \
            bash -lc '
              set -euo pipefail

              if [ -z "${KDIR:-}" ] || [ ! -f "$KDIR/Makefile" ]; then
                echo "KDIR is not available inside DDK container: ${KDIR:-<empty>}"
                exit 1
              fi

              echo "Building hymofs_lkm.ko with KDIR=$KDIR"
              (
                cd "$SRC_ROOT"
                make -j"$(nproc --all)" CC=clang \
                  CFLAGS_MODULE="-DHYMOFS_VERSION=\\\"0.1.${HYMOFS_VERSION}\\\""
              )

              mkdir -p "$OUT"
              KO_FILE="${KMI}_${TARGET_ARCH}_hymofs_lkm.ko"
              cp "$SRC/hymofs_lkm.ko" "$OUT/$KO_FILE"
              llvm-strip -d "$OUT/$KO_FILE" 2>/dev/null || true
              ls -la "$OUT/"
            '

      - name: Verify .ko
        run: |
          set -euo pipefail
          KO_FILE="${OUT}/${KMI}_${TARGET_ARCH}_hymofs_lkm.ko"
          test -s "$KO_FILE"
          ls -la "$KO_FILE"

      - name: Report cache hit
        if: steps.cache-lkm.outputs.cache-hit == 'true'
        run: |
          echo "Using cached ${KMI}_${TARGET_ARCH}_hymofs_lkm.ko"

      - name: Upload .ko
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.KMI }}-${{ env.TARGET_ARCH }}-hymofs-lkm
          path: ${{ env.OUT }}/${{ env.KMI }}_${{ env.TARGET_ARCH }}_hymofs_lkm.ko
